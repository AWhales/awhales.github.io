# Only production/ (and repo meta below) goes to git.
# Everything else stays local — Docs/, Game Assets/, WIP, etc.

/*
!/.gitignore
!/.github/
!/production/
!/README.md

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
