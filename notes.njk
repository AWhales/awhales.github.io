---
permalink: notes.html
---
<!DOCTYPE html>
<html lang="en">
{%- set pageTitle = "Notes — " + site.name -%}
{%- set description = "Dev notes and updates from making " + site.name + "." -%}
{%- set canonical = "/notes.html" -%}
{% include "head.njk" %}
<body class="page">
  <div class="page-sky">
    {% include "sky.njk" %}
  </div>

  <header class="topbar">
    <nav aria-label="Site">
      <a href="index.html">Home</a>
      <a href="notes.html" aria-current="page">Notes</a>
      <a href="contact.html">Contact</a>
    </nav>
  </header>

  <main class="page-main page-main--notes">
    <h1 class="page-title-art">
      <img src="/assets/notes_header.png" alt="Notes" width="50" height="10" draggable="false">
    </h1>

    <ul class="post-list">
      {%- for post in collections.posts -%}
      {%- set preview = post.data.preview or (post.templateContent | firstImage) -%}
      <li class="post-card">
        <a href="{{ post.url }}">
          {%- if preview %}
          <img class="post-card-preview" src="{{ preview | stillSrc }}" alt="">
          {%- endif %}
          <time datetime="{{ post.date | htmlDateString }}">{{ post.date | readableDate }}</time>
          <span class="post-card-title">
            {{ post.data.title }}
            {%- if post.data.wip %} <span class="post-badge">WIP</span>{%- endif %}
          </span>
          <span class="post-card-tease">{{ post.templateContent | firstParagraph }}</span>
        </a>
      </li>
      {%- endfor -%}
    </ul>

    {% set assetBase = "" %}
    {% include "newsletter.njk" %}
  </main>
  <script src="js/cloud-drag.js" defer></script>
</body>
</html>
